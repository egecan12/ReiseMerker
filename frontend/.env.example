# Frontend Environment Variables
# Copy this file to .env and update with your values

# Backend API URL (Required for Docker deployment)
BACKEND_URL=http://localhost:3000

# Production Backend URL (Update for production deployment)
# BACKEND_URL=https://your-backend-app.onrender.com

# Note: This file is used by docker-entrypoint.sh to replace
# localhost URLs in built JavaScript files at runtime.
# This is essential for OAuth to work correctly in production.
